paths-ignore:
  # Test fixtures and integration tests contain intentional fake secrets for validation
  - crates/chub-core/tests/
  # Benchmark scripts use realistic-looking tokens for timing (not real credentials)
  - scripts/benchmark-scan.sh
  # Source files that contain example/test tokens are already in .gitleaks.toml allowlist
  - crates/chub-core/src/team/tracking/redact.rs
  - crates/chub-core/src/scan/scanner.rs
